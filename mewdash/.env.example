# ========================================
# Mewdeko Dashboard Environment Variables
# ========================================
# Copy this file to .env and fill in your values.
# Required fields are marked with * REQUIRED.

# ========================================
# DISCORD OAUTH
# ========================================
# Create an OAuth2 application at https://discord.com/developers/applications
DISCORD_CLIENT_ID=""           # * REQUIRED
DISCORD_CLIENT_SECRET=""       # * REQUIRED
DISCORD_SCOPES="identify guilds"
DISCORD_REDIRECT_URI="http://localhost:5173/api/discord/callback"  # * REQUIRED - change to your public URL in production
DISCORD_API_URL="https://discordapp.com/api"

# ========================================
# SESSION / COOKIES
# ========================================
# Generate a secure random string (32+ characters)
COOKIE_ENCRYPTION_PASSWORD=""  # * REQUIRED

# ========================================
# BOT API CONNECTION
# ========================================
# The base URL of the running Mewdeko bot API (set in credentials.json ApiPort)
PUBLIC_MEWDEKO_API_URL="http://localhost:5001/botapi"  # * REQUIRED
# Must match ApiKey in the bot's credentials.json
MEWDEKO_API_KEY=""             # * REQUIRED

# ========================================
# JWT (Dashboard identity)
# ========================================
# Must match JwtSecret in the bot's credentials.json so the bot can verify dashboard user tokens
BOT_JWT_SECRET=""              # * REQUIRED

# ========================================
# REDIS (Session storage)
# ========================================
USE_REDIS=true
REDIS_URL="127.0.0.1"
# Must match the prefix used by the bot instance
REDIS_KEY=""                   # * REQUIRED if USE_REDIS=true

# ========================================
# NODE ENVIRONMENT
# ========================================
NODE_ENV=production

# ========================================
# OPTIONAL: Error tracking
# ========================================
# Sentry DSN is public/safe to expose client-side
PUBLIC_SENTRY_DSN=""
PUBLIC_SENTRY_ENVIRONMENT="production"

# ========================================
# OPTIONAL: Patreon integration
# ========================================
PATREON_CLIENT_ID=""
PATREON_CLIENT_SECRET=""

# ========================================
# OPTIONAL: top.gg integration
# ========================================
TOPGG_KEY=""

# ========================================
# OPTIONAL: Mobile apps and bot tokens
# ========================================
# Seconds a dashboard-minted bot token stays valid (default 300)
BOT_JWT_TTL="300"
# Secret used to sign mobile app sessions; required when the mobile apps sign in through this dashboard
MOBILE_JWT_SECRET=""
# Seconds a mobile access token stays valid (default 900) and a refresh token (default 30 days)
MOBILE_ACCESS_TOKEN_TTL="900"
MOBILE_REFRESH_TOKEN_TTL="2592000"
# OAuth redirect the mobile apps register with Discord; leave empty for the built-in default
MOBILE_OAUTH_REDIRECT_URI=""
# Review-mode sign in for app store reviewers; leave empty to disable
MOBILE_DEMO_CODE=""
MOBILE_DEMO_USER_ID=""
MOBILE_DEMO_USERNAME=""
# Shown to mobile users as this dashboard's name and its bot invite link
PUBLIC_INSTANCE_NAME="Mewdeko"
PUBLIC_BOT_INVITE_URL=""
